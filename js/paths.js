import {resolve} from 'path';

/** @type {string} Path to the partials folder */
export const partials = resolve(__dirname, '../partials');

/** @type {string} Path to the helpers folder */
export const helpers = resolve(__dirname, '../helpers');